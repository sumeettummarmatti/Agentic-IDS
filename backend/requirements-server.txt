# Minimal deps for simulated capture server containers (Dockerfile.server)
# Much lighter than the full IDS stack — no torch, no gymnasium, no LLM libs
confluent-kafka>=2.3.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
